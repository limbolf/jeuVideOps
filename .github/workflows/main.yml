name: FirstWorkflow
on:
  push:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  check-all-good:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: jeuVideOps
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
      - run: npm install
      - run: npm audit
